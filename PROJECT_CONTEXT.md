# Habit Tracker — Project Context

## Обзор проекта

Habit Tracker — локальный веб-трекер привычек для пары (Димочка 🦊 и Дашенька 🦋). 
Фокус на процессе, а не идеальном результате. Без давления и негатива.

**Ключевой принцип:** Лучше немного, чем идеально. Прогресс — это уже победа.

**Версия:** 3.2  
**Репозиторий:** https://github.com/Cosmovisioner/Habbit_tracker.git

---

## Техническая архитектура

### Стек
- **Один HTML файл** (`index.html`) со встроенными CSS и JavaScript (~7000 строк)
- **LocalStorage** для хранения данных (7 ключей)
- **Vanilla JS** — без фреймворков
- **PWA** — Progressive Web App для установки на телефон
- **Glassmorphism** — стекломорфный дизайн с blur эффектами

### Структура файлов
```
Habbit_tracker/
├── index.html          # Весь код приложения
├── manifest.json       # PWA манифест
├── sw.js               # Service Worker (v3)
├── icon-192.svg        # Иконка 192x192
├── icon-512.svg        # Иконка 512x512
├── README.md           # Описание проекта
├── PROJECT_CONTEXT.md  # Этот файл
├── CHAT_HANDOFF.md     # Краткий контекст для нового чата
└── .git/
```

---

## Модель данных (LocalStorage)

### habit_tracker_data
```javascript
{
  habits: [
    {
      id: "unique-id",
      name: "Медитация",
      type: "binary" | "numeric",
      period: "week" | "month" | "all",
      owner: "A" | "B" | "shared",
      frequency: "daily" | "weekly" | "custom",  // NEW v3.2
      frequencyTimes: 3,                          // NEW v3.2
      goal: 10,
      goalType: "daily" | "week" | "month" | "quarter" | "year" | "custom",
      customPeriod: 50,
      target: 100,
      category: "Здоровье"
    }
  ],
  entries: {
    "2026-02-01": {
      "habit-id": true,
      "habit-id-2": 15
    }
  },
  pausedDays: ["2026-01-15"]
}
```

### habit_tracker_settings
```javascript
{
  theme: "dark" | "light",
  pauseMode: false,
  pauseStartDate: null | "2026-01-30"
}
```

### habit_tracker_profiles
```javascript
{
  A: {
    name: "Димочка",
    avatar: "🦊",
    title: "Начинающий герой",
    superpower: "Несокрушимая воля",   // Одна строка
    traits: [                           // 5 слотов с иконками
      { icon: "💪", text: "Сила воли" },
      { icon: "🎯", text: "Фокус" },
      null, null, null
    ],
    bio: "О себе...",
    xp: 0,
    level: 1
  },
  B: { ... }
}
```

### Другие ключи
- `habit_tracker_achievements` — достижения пользователей
- `habit_tracker_challenges` — совместные челленджи
- `habit_tracker_sound` — звуки вкл/выкл (true/false)
- `habit_tracker_compact` — компактный режим (true/false)

---

## Реализованные функции

### Базовые функции
- ✅ Два типа привычек: бинарные (да/нет) и количественные
- ✅ Создание, редактирование, удаление привычек
- ✅ Владелец: Димочка, Дашенька или общая
- ✅ Категории/теги для привычек
- ✅ Drag-and-drop сортировка привычек
- ✅ Календарь с навигацией по месяцам

### Гибкие привычки (NEW v3.2)
- ✅ Частота: каждый день / раз в неделю / N раз в неделю
- ✅ Бейдж "2/3 на неделе" для не-ежедневных привычек
- ✅ Подсчёт выполнений за текущую неделю

### Компактный режим (NEW v3.2)
- ✅ Переключатель в Настройках → Внешний вид
- ✅ Скрывает прогресс-бары, владельцев, категории, календарь
- ✅ Оставляет только название + галочка для быстрого чекина

### Переключение пользователей
- ✅ Клик на карточку пользователя → показывает только его привычки
- ✅ Клик на "Общий прогресс" → показывает все привычки
- ✅ Индикатор текущего вида в шапке (кликабельный, cycling)
- ✅ Cycling view: клик на индикатор → Все → Димочка → Дашенька → Все

### RPG-геймификация
- ✅ Профили персонажей с аватаром (60+ эмодзи)
- ✅ Система уровней (Level N требует N*100 XP)
- ✅ XP за выполнение привычек (10 XP binary, до 20 XP numeric)
- ✅ Титул персонажа (редактируемый)
- ✅ Суперсила — одна строка, редактируется через prompt
- ✅ Черты характера — 5 слотов с иконками, Trait Picker modal
- ✅ Биография
- ✅ Кнопка "Мои привычки" в профиле

### Аналитика
- ✅ Модальное окно аналитики (📊)
- ✅ Недельный bar chart
- ✅ Месячный прогресс по привычкам
- ✅ Тепловая карта (12 недель)
- ✅ Общая статистика

### Habits Dashboard
- ✅ Полноэкранная таблица всех привычек на месяц
- ✅ Навигация по месяцам
- ✅ Кликабельные ячейки для быстрого отмечания
- ✅ Цветовая индикация (выполнено/частично/пропущено)

### Настройки
- ✅ Экспорт/импорт данных (JSON)
- ✅ Режим паузы/отпуска
- ✅ Переключатель темы
- ✅ Переключатель звуков
- ✅ Переключатель компактного режима
- ✅ Сброс прогресса / удаление всех данных

### Дизайн (Glassmorphism v3.2)
- ✅ Тёмная тема (по умолчанию) с анимированными цветными орбами
- ✅ Светлая тема
- ✅ Стекломорфизм: backdrop-filter blur, полупрозрачные карточки
- ✅ Мягкие тени и внутреннее свечение
- ✅ Адаптивная вёрстка

---

## Система достижений (20+ ачивок)

- 🌱 Первые шаги — создай первую привычку
- 🌿 Коллекционер — создай 5 привычек
- 🌳 Садовник — создай 10 привычек
- ✅ Начало положено — выполни первую привычку
- 🎯 На верном пути — выполни 10 привычек
- 💪 Настойчивость — выполни 50 привычек
- 🏆 Чемпион — выполни 100 привычек
- 🔥 Разогрев — серия 3 дня
- 🔥 Огненная неделя — серия 7 дней
- 🔥 Две недели силы — серия 14 дней
- 💎 Марафонец — серия 30 дней
- 👑 Легенда — серия 100 дней
- ⭐ Ученик — уровень 5
- 🌟 Мастер — уровень 10
- 💫 Грандмастер — уровень 25
- 📅 Активная неделя — 7 активных дней
- 📆 Активный месяц — 30 активных дней
- 💕 Командная работа — общая привычка
- 🎭 Индивидуальность — заполнен профиль
- 🌅 Ранняя пташка — выполнение до 7:00
- 🦉 Полуночник — выполнение после 23:00

---

## Ключевые функции в коде

### Data Layer
- `loadData()` / `saveData(data)`
- `loadSettings()` / `saveSettings(settings)`
- `loadProfiles()` / `saveProfiles(profiles)`
- `loadAchievements()` / `saveAchievements(achievements)`
- `loadChallenges()` / `saveChallenges(challenges)`

### User View
- `switchUserView(userId)` — переключение вида (A/B/all)
- `cycleView()` — циклическое переключение вида
- `updateViewIndicator()` — обновление индикатора в шапке

### Habits
- `renderHabits()` — отрисовка списка
- `getWeeklyCompletions(habitId)` — подсчёт выполнений за неделю (NEW)
- `toggleBinaryHabit(habitId)`
- `updateNumericHabit(habitId, value)`

### Character Profiles
- `showProfile(userId)` — открытие профиля
- `goToMyHabits()` — переход к привычкам из профиля
- `editSuperpower()` — редактирование суперсилы
- `openTraitPicker(slotIndex)` — выбор черты характера
- `renderTraits(userId)` — отрисовка 5 слотов черт

### Compact Mode (NEW)
- `applyCompactMode()` — применение режима
- `toggleCompactMode()` — переключение
- `updateCompactModeUI()` — обновление UI

### Habits Dashboard
- `openHabitsDashboard()` — открытие дашборда
- `renderDashboard()` — отрисовка таблицы
- `toggleDashboardCell(habitId, dateStr, type)` — переключение ячейки

---

## История версий

### v3.2 (Февраль 2026)
- Glassmorphism дизайн (blur, орбы, стеклянные карточки)
- Гибкие привычки (N раз в неделю)
- Компактный режим

### v3.1 (Февраль 2026)
- Habits Dashboard — таблица всех привычек на месяц
- Cycling View — переключение вида кликом на индикатор
- Trait Picker — 5 слотов черт характера с иконками
- Кнопка "Мои привычки" в профиле
- SVG иконки в header

### v3.0 (Январь 2026)
- Система достижений (20+ ачивок)
- Награды за серии (огоньки, бейджи)
- Inline-редактирование профиля
- Совместные челленджи
- Анимации (конфетти, level-up overlay)
- Звуковые эффекты

### v2.1 (Январь 2026)
- Переключение видов по пользователям
- RPG-профили персонажей

### v2.0 (Январь 2026)
- PWA, аналитика, темы, категории, drag-drop

### v1.0 (Январь 2026)
- MVP: привычки, календарь, базовый прогресс

---

## Пользователи

- **Димочка 🦊** (User A) — розовый цвет
- **Дашенька 🦋** (User B) — фиолетовый цвет
- **Общие привычки** — для обоих

---

*Последнее обновление: Февраль 2026, v3.2*

# Habit Tracker — Project Context

## Обзор проекта

Habit Tracker — локальный веб-трекер привычек для пары (Димочка 🦊 и Дашенька 🦋). 
Фокус на процессе, а не идеальном результате. Без давления и негатива.

**Ключевой принцип:** Лучше немного, чем идеально. Прогресс — это уже победа.

**Версия:** 2.1  
**Репозиторий:** https://github.com/Cosmovisioner/Habbit_tracker.git

---

## Техническая архитектура

### Стек
- **Один HTML файл** (`index.html`) со встроенными CSS и JavaScript (~4500 строк)
- **LocalStorage** для хранения данных (3 ключа)
- **Vanilla JS** — без фреймворков
- **PWA** — Progressive Web App для установки на телефон

### Структура файлов
```
Habbit_tracker/
├── index.html          # Весь код приложения
├── manifest.json       # PWA манифест
├── sw.js               # Service Worker
├── icon-192.svg        # Иконка 192x192
├── icon-512.svg        # Иконка 512x512
├── README.md           # Описание проекта
├── PROJECT_CONTEXT.md  # Этот файл
└── .git/
```

---

## Модель данных (LocalStorage)

### habit_tracker_data
```javascript
{
  habits: [
    {
      id: "unique-id",
      name: "Медитация",
      type: "binary" | "numeric",
      period: "week" | "month" | "all",
      owner: "A" | "B" | "shared",
      goal: 10,                          // Для numeric
      goalType: "daily" | "week" | "month" | "quarter" | "year" | "custom",
      customPeriod: 50,                  // Для custom
      target: 100,                       // Общая цель
      category: "Здоровье"               // Категория
    }
  ],
  entries: {
    "2026-01-30": {
      "habit-id": true,      // binary
      "habit-id-2": 15       // numeric
    }
  },
  pausedDays: ["2026-01-15"]
}
```

### habit_tracker_settings
```javascript
{
  theme: "dark" | "light",
  pauseMode: false,
  pauseStartDate: null | "2026-01-30"
}
```

### habit_tracker_profiles
```javascript
{
  A: {
    name: "Димочка",
    avatar: "🦊",
    title: "Начинающий герой",
    superpowerIcon: "🌟",
    superpowerName: "Не задана",
    superpowerDesc: "Описание суперсилы",
    traits: ["Целеустремлённость"],
    bio: "О себе...",
    xp: 0,
    level: 1
  },
  B: { ... }
}
```

---

## Реализованные функции

### Базовые функции
- ✅ Два типа привычек: бинарные (да/нет) и количественные
- ✅ Создание, редактирование, удаление привычек
- ✅ Владелец: Димочка, Дашенька или общая
- ✅ Категории/теги для привычек
- ✅ Drag-and-drop сортировка привычек
- ✅ Календарь с навигацией по месяцам

### Переключение пользователей
- ✅ Клик на карточку пользователя → показывает только его привычки
- ✅ Клик на "Общий прогресс" → показывает все привычки
- ✅ Индикатор текущего вида в шапке
- ✅ Активная карточка подсвечивается

### RPG-геймификация
- ✅ Профили персонажей с аватаром (60+ эмодзи)
- ✅ Система уровней (Level N требует N*100 XP)
- ✅ XP за выполнение привычек (10 XP binary, до 20 XP numeric)
- ✅ Титул персонажа (редактируемый)
- ✅ Суперсила (иконка + название + описание)
- ✅ Черты характера (теги)
- ✅ Биография

### Цели и прогресс
- ✅ Периоды цели: в день, неделю, месяц, квартал, год, свой
- ✅ Общая цель (target) с бейджем прогресса
- ✅ Прогресс-бары для каждой привычки
- ✅ Глобальный прогресс-бар с анимацией

### Детальный просмотр привычки
- ✅ Статистика: выполнено дней, текущая/лучшая серия
- ✅ Календарь выполнений с навигацией

### Аналитика
- ✅ Модальное окно аналитики (📊)
- ✅ Недельный bar chart
- ✅ Месячный прогресс по привычкам
- ✅ Тепловая карта (12 недель)
- ✅ Общая статистика (привычки, выполнения, серии)

### Настройки
- ✅ Экспорт/импорт данных (JSON)
- ✅ Режим паузы/отпуска
- ✅ Переключатель темы
- ✅ Сброс прогресса / удаление всех данных

### PWA
- ✅ manifest.json
- ✅ Service Worker (офлайн кэширование)
- ✅ SVG иконки

### Дизайн
- ✅ Тёмная тема (по умолчанию)
- ✅ Светлая тема
- ✅ Градиенты, glassmorphism
- ✅ Неоновое свечение
- ✅ Адаптивная вёрстка

---

## Не реализовано (идеи)

- ⬜ Достижения/ачивки
- ⬜ Push-уведомления
- ⬜ Облачная синхронизация
- ⬜ Виджеты
- ⬜ Совместные челленджи
- ⬜ Награды за серии

---

## Ключевые функции в коде

### Data Layer
- `loadData()` / `saveData(data)`
- `loadSettings()` / `saveSettings(settings)`
- `loadProfiles()` / `saveProfiles(profiles)`
- `generateId()`

### User View
- `switchUserView(userId)` — переключение вида (A/B/all)
- `updateViewIndicator()` — обновление индикатора в шапке
- `updateUserCards()` — обновление карточек с XP/уровнем

### Character Profiles
- `showProfile(userId)` — открытие профиля
- `toggleProfileEdit(editMode)` — режим редактирования
- `saveProfile()` — сохранение профиля
- `openEmojiPicker()` / `selectAvatar(emoji)`
- `calculateUserXP(userId)` — подсчёт XP
- `calculateLevel(xp)` — расчёт уровня
- `calculateUserStats(userId)` — статистика пользователя

### Habits
- `renderHabits()` — отрисовка списка
- `toggleBinaryHabit(habitId)` / `updateNumericHabit(habitId, value)`
- `editHabit(habitId)` / `deleteHabit(habitId)`
- `setupDragAndDrop()`

### Categories
- `getCategories()` / `renderCategoryFilter()`

### Progress
- `calculateHabitProgress(habit)`
- `updateProgressBars()`
- `countTotalCompletions(habit)`

### Analytics
- `renderAnalytics()` / `renderAnalyticsStats()`
- `renderWeeklyChart()` / `renderMonthlyChart()` / `renderHeatmap()`

### Theme & Settings
- `applyTheme(theme)` / `toggleTheme()`
- `togglePauseMode()` / `updatePauseModeUI()`

---

## История коммитов

```
e826c5a feat: Separate habits view by user with clickable cards
bc89a3a feat: Add RPG-style character profiles with levels and XP
1614589 feat: Major update — PWA, analytics, themes, categories, drag-drop
e7266de docs: Add PROJECT_CONTEXT.md for chat continuity
279d3c6 feat: Unified colors + multiple goal periods
7a35024 style: Improve numeric input UX
2b44445 feat: Add lifetime target goal for habits
c826591 feat: Add month navigation to habit detail calendar
d3f7011 feat: Visual feedback for completed habits + habit detail modal
7765d2b feat: Edit habits + daily/total goals for numeric habits
233c5a8 style: Dark theme with pink gradients + rename users
aeb7ada feat: Initial MVP of Habit Tracker
```

---

## Пользователи

- **Димочка 🦊** (User A) — розовый цвет
- **Дашенька 🦋** (User B) — фиолетовый цвет
- **Общие привычки** — для обоих

---

## Как продолжить разработку

1. Откройте `index.html` — весь код там
2. CSS: строки 14-2150
3. HTML: строки 2150-2460
4. JavaScript: строки 2460-4410

### Структура JavaScript:
- DATA LAYER (2463-2525)
- STATE (2527-2537)
- UTILS (2539-2570)
- CALENDAR (2572-2625)
- CATEGORIES (2627-2665)
- HABITS (2667-2830)
- DRAG AND DROP (2832-2880)
- PROGRESS CALCULATION (2940-3010)
- ADD/EDIT HABIT FORM (3012-3180)
- USER VIEW SWITCHING (3535-3575)
- THEME (3580-3620)
- PAUSE MODE (3622-3660)
- SETTINGS MODAL (3662-3700)
- EXPORT/IMPORT (3702-3780)
- ANALYTICS (3800-4010)
- CHARACTER PROFILES (4012-4260)
- PWA (4380-4390)
- INIT (4391-4410)

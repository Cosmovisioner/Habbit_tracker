# Habit Tracker — Project Context

## Обзор проекта

Habit Tracker — локальный веб-трекер привычек для пары (Димочка 🦊 и Дашенька 🦋). 
Фокус на процессе, а не идеальном результате. Без давления и негатива.

**Ключевой принцип:** Лучше немного, чем идеально. Прогресс — это уже победа.

---

## Техническая архитектура

### Стек
- **Один HTML файл** (`index.html`) со встроенными CSS и JavaScript
- **LocalStorage** для хранения данных
- **Vanilla JS** — без фреймворков
- **Git репозиторий:** https://github.com/Cosmovisioner/Habbit_tracker.git

### Структура файлов
```
Habbit_tracker/
├── index.html          # Весь код приложения
├── PROJECT_CONTEXT.md  # Этот файл
└── .git/
```

---

## Модель данных (LocalStorage)

```javascript
{
  habits: [
    {
      id: "unique-id",
      name: "Медитация",
      type: "binary" | "numeric",
      period: "week" | "month" | "all",      // Период отслеживания прогресса
      owner: "A" | "B" | "shared",           // Димочка, Дашенька или общая
      goal: 10,                               // Для numeric: цель за период
      goalType: "daily" | "week" | "month" | "quarter" | "year" | "custom",
      customPeriod: 50,                       // Для custom: количество дней
      target: 100                             // Общая цель (например, 100 медитаций за год)
    }
  ],
  entries: {
    "2026-01-30": {
      "habit-id": true,      // binary: выполнено
      "habit-id-2": 15       // numeric: значение
    }
  }
}
```

---

## Реализованные функции

### UI компоненты
- ✅ Header с названием и переключателем пользователей (Все / Димочка 🦊 / Дашенька 🦋)
- ✅ Глобальный прогресс-бар с анимацией свечения
- ✅ Календарь с навигацией по месяцам и подсветкой активных дней
- ✅ Список привычек с чекбоксами (binary) и числовыми полями (numeric)
- ✅ Прогресс-бары для каждой привычки
- ✅ Индивидуальные прогресс-бары для каждого пользователя
- ✅ Мотивирующие цитаты внизу страницы

### Привычки
- ✅ Два типа: бинарные (да/нет) и количественные (число)
- ✅ Создание новых привычек через форму
- ✅ Редактирование существующих привычек (кнопка ✎)
- ✅ Удаление привычек (кнопка ×)
- ✅ Владелец: Димочка, Дашенька или общая

### Количественные привычки
- ✅ Периоды цели: в день, в неделю, в месяц, в квартал, в год, свой период
- ✅ Скрыты стрелки на числовом поле (только ручной ввод)
- ✅ Розовая подсветка при выполнении цели
- ✅ Красная подсветка при частичном выполнении (> 0 но < цели)

### Общая цель (Target)
- ✅ Опциональная общая цель для любой привычки (например, 100 медитаций)
- ✅ Бейдж прогресса на карточке (23/100 с мини-баром)
- ✅ Большой прогресс-бар в модальном окне детали

### Детальный просмотр привычки
- ✅ Клик на название привычки открывает модальное окно
- ✅ Статистика: выполнено дней, текущая серия, лучшая серия (за 90 дней)
- ✅ Календарь с подсветкой выполненных/пропущенных дней
- ✅ Навигация по месяцам в модальном окне

### Дизайн
- ✅ Тёмная тема с градиентным фоном (#0f0f1a → #1a1a2e → #16213e)
- ✅ Розово-фиолетовые градиенты (glassmorphism)
- ✅ Неоновое свечение на активных элементах
- ✅ Адаптивная вёрстка (mobile-friendly)

---

## НЕ реализовано (потенциальные улучшения)

### Из оригинального PRD
- ⬜ Синхронизация через JSON файл (File System Access API / drag-and-drop)
- ⬜ Режимы: Minimum Mode (сниженные ожидания), Pause/Vacation
- ⬜ Аналитика и графики
- ⬜ Экспорт данных

### Возможные улучшения
- ⬜ Удаление данных / сброс прогресса
- ⬜ Сортировка привычек (drag-and-drop)
- ⬜ Категории/теги для привычек
- ⬜ Напоминания (без давления, опционально)
- ⬜ PWA (Progressive Web App) для установки на телефон
- ⬜ Тёмная/светлая тема (переключатель)

---

## История коммитов

```
279d3c6 feat: Unified colors + multiple goal periods
7a35024 style: Improve numeric input UX
2b44445 feat: Add lifetime target goal for habits
c826591 feat: Add month navigation to habit detail calendar
d3f7011 feat: Visual feedback for completed habits + habit detail modal
7765d2b feat: Edit habits + daily/total goals for numeric habits
233c5a8 style: Dark theme with pink gradients + rename users
aeb7ada feat: Initial MVP of Habit Tracker
```

---

## Ключевые функции в коде

### Data Layer
- `loadData()` / `saveData(data)` — работа с LocalStorage
- `generateId()` — генерация уникальных ID

### Calendar
- `renderCalendar()` — отрисовка календаря текущего месяца
- `formatDate(date)` — форматирование даты в YYYY-MM-DD

### Habits
- `renderHabits()` — отрисовка списка привычек
- `toggleBinaryHabit(habitId)` — переключение бинарной привычки
- `updateNumericHabit(habitId, value)` — обновление числовой привычки
- `editHabit(habitId)` — редактирование привычки
- `deleteHabit(habitId)` — удаление привычки

### Progress
- `calculateHabitProgress(habit)` — расчёт прогресса за период
- `updateProgressBars()` — обновление всех прогресс-баров
- `countTotalCompletions(habit)` — подсчёт общего количества выполнений
- `checkNumericGoalCompleted(habit, numValue)` — проверка выполнения цели

### Habit Detail Modal
- `showHabitDetail(habitId)` — открытие модального окна
- `calculateHabitStats(habit)` — расчёт статистики (серии, выполнения)
- `renderHabitStats(stats, habit)` — отрисовка статистики
- `renderHabitDetailCalendar(habit)` — отрисовка календаря в модалке

### Helpers
- `getPeriodLabel(period)` — метка периода (неделю, месяц, всё время)
- `getGoalPeriodLabel(goalType, customPeriod)` — метка периода цели
- `getGoalPeriodDays(goalType, customPeriod)` — количество дней в периоде

---

## Психологические ограничения (из PRD)

### Запрещено
- ❌ Жёсткие напоминания
- ❌ Красные индикаторы провала (используем мягкий красный только для "не дотянул")
- ❌ Сообщения о провале
- ❌ Сравнение пользователей
- ❌ Рейтинги и лидерборды

### Обязательно
- ✅ Поддерживающие микротексты (мотивирующие цитаты)
- ✅ Фокус на процессе
- ✅ Нейтральная визуальная обратная связь
- ✅ Возможность пропускать дни без наказаний

---

## Как запустить

1. Откройте `index.html` в браузере (двойной клик или `open index.html`)
2. Данные сохраняются автоматически в LocalStorage браузера

---

## Пользователи

- **Димочка 🦊** (User A) — розовый цвет
- **Дашенька 🦋** (User B) — фиолетовый цвет
- **Общие привычки** — для обоих

# Habit Tracker — Project Context

## Обзор проекта

Habit Tracker — локальный веб-трекер привычек для пары (Димочка 🦊 и Дашенька 🦋). 
Фокус на процессе, а не идеальном результате. Без давления и негатива.

**Ключевой принцип:** Лучше немного, чем идеально. Прогресс — это уже победа.

**Версия:** 3.0  
**Репозиторий:** https://github.com/Cosmovisioner/Habbit_tracker.git

---

## Техническая архитектура

### Стек
- **Один HTML файл** (`index.html`) со встроенными CSS и JavaScript (~5500 строк)
- **LocalStorage** для хранения данных (5 ключей)
- **Vanilla JS** — без фреймворков
- **PWA** — Progressive Web App для установки на телефон

### Структура файлов
```
Habbit_tracker/
├── index.html          # Весь код приложения
├── manifest.json       # PWA манифест
├── sw.js               # Service Worker
├── icon-192.svg        # Иконка 192x192
├── icon-512.svg        # Иконка 512x512
├── README.md           # Описание проекта
├── PROJECT_CONTEXT.md  # Этот файл
├── CHAT_HANDOFF.md     # Краткий контекст для нового чата
└── .git/
```

---

## Модель данных (LocalStorage)

### habit_tracker_data
```javascript
{
  habits: [
    {
      id: "unique-id",
      name: "Медитация",
      type: "binary" | "numeric",
      period: "week" | "month" | "all",
      owner: "A" | "B" | "shared",
      goal: 10,                          // Для numeric
      goalType: "daily" | "week" | "month" | "quarter" | "year" | "custom",
      customPeriod: 50,                  // Для custom
      target: 100,                       // Общая цель
      category: "Здоровье"               // Категория
    }
  ],
  entries: {
    "2026-01-30": {
      "habit-id": true,      // binary
      "habit-id-2": 15       // numeric
    }
  },
  pausedDays: ["2026-01-15"]
}
```

### habit_tracker_settings
```javascript
{
  theme: "dark" | "light",
  pauseMode: false,
  pauseStartDate: null | "2026-01-30"
}
```

### habit_tracker_profiles
```javascript
{
  A: {
    name: "Димочка",
    avatar: "🦊",
    title: "Начинающий герой",
    superpowerIcon: "🌟",
    superpowerName: "Не задана",
    superpowerDesc: "Описание суперсилы",
    traits: ["Целеустремлённость"],
    bio: "О себе...",
    xp: 0,
    level: 1
  },
  B: { ... }
}
```

### habit_tracker_achievements (NEW v3.0)
```javascript
{
  A: {
    "first_habit": { unlockedAt: "2026-01-30T10:00:00Z", seen: true },
    "streak_7": { unlockedAt: "2026-01-30T10:00:00Z", seen: false }
  },
  B: { ... }
}
```

### habit_tracker_challenges (NEW v3.0)
```javascript
[
  {
    id: "unique-id",
    name: "100км вместе",
    goal: 100,
    unit: "км",
    duration: 30,
    startDate: "2026-01-30T10:00:00Z",
    progressA: 25,
    progressB: 30
  }
]
```

---

## Реализованные функции

### Базовые функции
- ✅ Два типа привычек: бинарные (да/нет) и количественные
- ✅ Создание, редактирование, удаление привычек
- ✅ Владелец: Димочка, Дашенька или общая
- ✅ Категории/теги для привычек
- ✅ Drag-and-drop сортировка привычек
- ✅ Календарь с навигацией по месяцам

### Переключение пользователей
- ✅ Клик на карточку пользователя → показывает только его привычки
- ✅ Клик на "Общий прогресс" → показывает все привычки
- ✅ Индикатор текущего вида в шапке
- ✅ Активная карточка подсвечивается

### RPG-геймификация
- ✅ Профили персонажей с аватаром (60+ эмодзи)
- ✅ Система уровней (Level N требует N*100 XP)
- ✅ XP за выполнение привычек (10 XP binary, до 20 XP numeric)
- ✅ Титул персонажа (редактируемый)
- ✅ Суперсила (иконка + название + описание)
- ✅ Черты характера (теги)
- ✅ Биография

### Цели и прогресс
- ✅ Периоды цели: в день, неделю, месяц, квартал, год, свой
- ✅ Общая цель (target) с бейджем прогресса
- ✅ Прогресс-бары для каждой привычки
- ✅ Глобальный прогресс-бар с анимацией

### Детальный просмотр привычки
- ✅ Статистика: выполнено дней, текущая/лучшая серия
- ✅ Календарь выполнений с навигацией

### Аналитика
- ✅ Модальное окно аналитики (📊)
- ✅ Недельный bar chart
- ✅ Месячный прогресс по привычкам
- ✅ Тепловая карта (12 недель)
- ✅ Общая статистика (привычки, выполнения, серии)

### Настройки
- ✅ Экспорт/импорт данных (JSON)
- ✅ Режим паузы/отпуска
- ✅ Переключатель темы
- ✅ Переключатель звуков (NEW v3.0)
- ✅ Сброс прогресса / удаление всех данных

### PWA
- ✅ manifest.json
- ✅ Service Worker (офлайн кэширование)
- ✅ SVG иконки

### Дизайн
- ✅ Тёмная тема (по умолчанию)
- ✅ Светлая тема
- ✅ Градиенты, glassmorphism
- ✅ Неоновое свечение
- ✅ Адаптивная вёрстка

---

## Новые функции v3.0

### 🏆 Система достижений (Achievements)
- ✅ 20+ достижений разных категорий
- ✅ Автоматическая проверка при выполнении привычек
- ✅ Popup-уведомление при разблокировке
- ✅ Секция достижений в профиле (НЕ редактируемая)
- ✅ Tooltip с описанием и датой получения
- ✅ Анимация пульсации для новых достижений

**Список достижений:**
- 🌱 Первые шаги — создай первую привычку
- 🌿 Коллекционер — создай 5 привычек
- 🌳 Садовник — создай 10 привычек
- ✅ Начало положено — выполни первую привычку
- 🎯 На верном пути — выполни 10 привычек
- 💪 Настойчивость — выполни 50 привычек
- 🏆 Чемпион — выполни 100 привычек
- 🔥 Разогрев — серия 3 дня
- 🔥 Огненная неделя — серия 7 дней
- 🔥 Две недели силы — серия 14 дней
- 💎 Марафонец — серия 30 дней
- 👑 Легенда — серия 100 дней
- ⭐ Ученик — уровень 5
- 🌟 Мастер — уровень 10
- 💫 Грандмастер — уровень 25
- 📅 Активная неделя — 7 активных дней
- 📆 Активный месяц — 30 активных дней
- 💕 Командная работа — общая привычка
- 🎭 Индивидуальность — заполнен профиль
- 🌅 Ранняя пташка — выполнение до 7:00
- 🦉 Полуночник — выполнение после 23:00

### 🔥 Награды за серии (Streak Rewards)
- ✅ Огоньки рядом с привычками (🔥3, 🔥7 и т.д.)
- ✅ Бейджи серий на карточках пользователей
- ✅ 4 уровня: bronze (3+), silver (7+), gold (30+), diamond (100+)
- ✅ Анимация свечения для gold и diamond

### ✏️ Inline-редактирование профиля
- ✅ Клик на любое поле профиля → редактирование на месте
- ✅ Имя, титул — inline input
- ✅ Суперсила (иконка, название, описание) — inline
- ✅ Черты характера — inline с подсказкой
- ✅ Биография — inline textarea
- ✅ Достижения — НЕ редактируются (только зарабатываются)
- ✅ Сохранение при blur или Enter

### 🎉 Анимации
- ✅ Конфетти при повышении уровня
- ✅ Overlay "Новый уровень!" с анимацией
- ✅ Popup для разблокированных достижений
- ✅ Пульсация новых достижений
- ✅ Свечение gold/diamond бейджей

### 🤝 Совместные челленджи
- ✅ Создание челленджей с целью, единицей, длительностью
- ✅ Прогресс для каждого пользователя отдельно
- ✅ Двухцветный прогресс-бар (розовый + фиолетовый)
- ✅ Обратный отсчёт дней
- ✅ Кнопки "+1" для каждого пользователя
- ✅ Удаление челленджей

### 🔊 Звуковые эффекты
- ✅ Звук при выполнении привычки
- ✅ Звук при повышении уровня (фанфары)
- ✅ Звук при получении достижения
- ✅ Переключатель в настройках
- ✅ Генерация через Web Audio API (без файлов)

---

## Ключевые функции в коде

### Data Layer
- `loadData()` / `saveData(data)`
- `loadSettings()` / `saveSettings(settings)`
- `loadProfiles()` / `saveProfiles(profiles)`
- `loadAchievements()` / `saveAchievements(achievements)` (NEW)
- `loadChallenges()` / `saveChallenges(challenges)` (NEW)
- `generateId()`

### User View
- `switchUserView(userId)` — переключение вида (A/B/all)
- `updateViewIndicator()` — обновление индикатора в шапке
- `updateUserCards()` — обновление карточек с XP/уровнем/бейджами

### Character Profiles
- `showProfile(userId)` — открытие профиля
- `startInlineEdit(element, field, isTextarea)` — inline редактирование (NEW)
- `finishInlineEdit(input, field)` — завершение редактирования (NEW)
- `startTraitsEdit()` — редактирование черт (NEW)
- `openEmojiPicker()` / `selectAvatar(emoji)`
- `calculateUserXP(userId)` — подсчёт XP
- `calculateLevel(xp)` — расчёт уровня
- `calculateUserStats(userId)` — статистика пользователя

### Achievements (NEW)
- `checkAchievements(userId)` — проверка и разблокировка
- `showAchievementPopup(achievement)` — popup уведомление
- `renderAchievements(userId)` — отрисовка в профиле
- `checkTimeBasedAchievements(userId)` — ранняя пташка/полуночник

### Streaks (NEW)
- `getStreakBadge(streak)` — получение бейджа серии
- `getHabitStreak(habitId)` — расчёт серии привычки
- `getStreakFireClass(streak)` — CSS класс огонька

### Challenges (NEW)
- `renderChallenges()` — отрисовка челленджей
- `createChallenge()` — создание нового
- `addChallengeProgress(id, userId)` — добавление прогресса
- `deleteChallenge(id)` — удаление

### Animations (NEW)
- `createConfetti()` — анимация конфетти
- `showLevelUp(level)` — overlay повышения уровня
- `closeLevelUp()` — закрытие overlay

### Sounds (NEW)
- `playSound(type)` — воспроизведение звука
- `toggleSound()` — переключение звуков
- `updateSoundToggleUI()` — обновление UI

### Habits
- `renderHabits()` — отрисовка списка (теперь с огоньками)
- `toggleBinaryHabit(habitId)` — (обновлено: звуки, ачивки, level-up)
- `updateNumericHabit(habitId, value)` — (обновлено: звуки, ачивки)
- `editHabit(habitId)` / `deleteHabit(habitId)`
- `setupDragAndDrop()`

### Categories
- `getCategories()` / `renderCategoryFilter()`

### Progress
- `calculateHabitProgress(habit)`
- `updateProgressBars()`
- `countTotalCompletions(habit)`

### Analytics
- `renderAnalytics()` / `renderAnalyticsStats()`
- `renderWeeklyChart()` / `renderMonthlyChart()` / `renderHeatmap()`

### Theme & Settings
- `applyTheme(theme)` / `toggleTheme()`
- `togglePauseMode()` / `updatePauseModeUI()`

---

## Пользователи

- **Димочка 🦊** (User A) — розовый цвет
- **Дашенька 🦋** (User B) — фиолетовый цвет
- **Общие привычки** — для обоих

---

## История версий

### v3.0 (Январь 2026)
- Система достижений (20+ ачивок)
- Награды за серии (огоньки, бейджи)
- Inline-редактирование профиля
- Совместные челленджи
- Анимации (конфетти, level-up overlay)
- Звуковые эффекты

### v2.1 (Январь 2026)
- Переключение видов по пользователям
- RPG-профили персонажей

### v2.0 (Январь 2026)
- PWA, аналитика, темы, категории, drag-drop

### v1.0 (Январь 2026)
- MVP: привычки, календарь, базовый прогресс

---

## Как продолжить разработку

1. Откройте `index.html` — весь код там
2. CSS: строки 14-2750
3. HTML: строки 2750-3170
4. JavaScript: строки 3170-5610

### Структура JavaScript:
- DATA LAYER (3170-3270)
- ACHIEVEMENTS DEFINITIONS (3270-3330)
- SOUND SYSTEM (3330-3400)
- STATE (3400-3420)
- CONFETTI & LEVEL UP (3420-3500)
- ACHIEVEMENTS SYSTEM (3500-3600)
- STREAK BADGES (3600-3650)
- INLINE EDITING (3650-3780)
- CHALLENGES SYSTEM (3780-3950)
- UTILS (3950-4000)
- CALENDAR (4000-4100)
- CATEGORIES (4100-4150)
- HABITS (4150-4350)
- DRAG AND DROP (4350-4420)
- PROGRESS CALCULATION (4420-4550)
- ADD/EDIT HABIT FORM (4550-4750)
- USER VIEW SWITCHING (5200-5250)
- THEME (5250-5300)
- PAUSE MODE (5300-5350)
- SETTINGS MODAL (5350-5400)
- EXPORT/IMPORT (5400-5480)
- ANALYTICS (5480-5700)
- CHARACTER PROFILES (5700-5900)
- PWA (5900-5920)
- INIT (5920-5610)

---

*Последнее обновление: Январь 2026, v3.0*
